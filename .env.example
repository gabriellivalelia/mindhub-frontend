
# MindHub Frontend - Environment Variables

# ============================================================================
# FRONTEND / API Configuration
# ============================================================================
# URL base da API do backend
# Desenvolvimento: http://localhost:8000
# Produção: https://api.mindhub.com.br (exemplo)
VITE_API_URL=http://localhost:8000


# ============================================================================
# BACKEND SHARED ENV (copied from backend .env.example)
# These values are provided so the user doesn't need to create a separate
# backend `.env` when running both services locally. They are informational
# defaults and can be overridden in a real deployment.
# ============================================================================

# APP CONFIG
APP_VERSION=1
ENV=development
PORT=8000
HOST=0.0.0.0
LOG_LEVEL=info
FILES_PATH=./temp

# DATABASE CONFIG - MongoDB
# URL de conexão do MongoDB
MONGO_URI=mongodb://localhost:27017

# Nome do banco de dados
MONGO_DATABASE_NAME=mindhub-dev

# REDIS CONFIG
# Host e porta do Redis (usado para cache e sessões)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT CONFIG
# Algoritmo de criptografia
ALGORITHM=HS256

# Secret para assinar tokens de acesso (MUDE EM PRODUÇÃO!)
ACCESS_TOKEN_SECRET=seu_secret_super_seguro_aqui_mude_em_producao

# Tempo de expiração do access token em minutos (24h = 1440 minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Tempo de expiração do refresh token em segundos (24h = 86400 segundos)
REFRESH_TOKEN_EXPIRE_SECONDS=86400

# Notes:
# - The frontend uses VITE_API_URL to talk to the backend. The other variables
#   are provided for convenience when running both services locally (e.g., via
#   docker-compose). They are not directly consumed by the frontend runtime,
#   but keeping them in a shared `.env.example` helps new contributors.
