// Re-export the schedule component styles so addAvailabilities can use the same style tokens
export {
  ScheduleWrapper,
  ScheduleGrid,
  HeaderRow,
  SlotsRowContainer,
  SlotsRow,
  ArrowButton,
  DayColumn,
  DayHeader,
  DayName,
  DayNumber,
  TimeSlot,
  ScrollBar,
} from "../scheduleComponent/styles";

export default {};
